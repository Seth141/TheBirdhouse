# Railway service for the birdhouse inference loop.
# Deploy this folder as its own Railway service (Root Directory: inference-service).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
